# .env 文件 — Docker Compose 环境变量
# 复制此文件为 .env 并按实际情况修改

# 管理员后台密码（必改！）
ADMIN_TOKEN=tianji-admin-2026

# 后端 API 地址（浏览器端能访问的地址，改为你的域名）
# 如果后端和前端都在同一台服务器，格式为：https://你的域名/api
# 如果后端暴露独立域名：https://api.你的域名
NEXT_PUBLIC_API_URL=http://your-domain.com:8000
